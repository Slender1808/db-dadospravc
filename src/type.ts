// import package mime-types
import mime from "mime-types"

// get image mime type
var mimeType = mime.lookup("/home/jadson/Documentos/estados.json");

// print image mime type
console.log(mimeType);
